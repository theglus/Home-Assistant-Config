#################################################################
## Fan Popup
#################################################################
template:
  - base
  - circle
  - loader
variables:
  circle_input: >
    [[[
    return entity === undefined ?
      null :
      parseInt(entity.attributes.percentage);
    ]]]
hold_action:
  action: failure
double_tap_action:
  action: more-info
